{
  "name": "alu-launchpad-tooling",
  "private": true,
  "version": "1.0.0",
  "description": "Node.js dev tooling for ALU Launchpad (Firestore demo/dev seed script). Not part of the Flutter app itself.",
  "scripts": {
    "seed:firestore": "node scripts/seed_firestore.js"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0"
  }
}
